namespace ProceduralRts.Core;

public enum CommandPreviewKind
{
    None,
    Select,
    DragSelect,
    TargetHover,
    Move,
    Attack,
    Repair,
    Rally,
    Harvest,
    BuildValid,
    BuildInvalid,
}
