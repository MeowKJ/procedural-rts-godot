# Review Record - 1v1 mode scope

Step: Lock the current vertical slice to one human player versus one computer AI.
Milestone: Playable 1v1 Skirmish - scope locks.
Owner AI: Codex.
Reviewer AI: Codex self-review (CombatBehavior and ReviewGate provide durable checks).
Integrator AI: Codex.

Scope:
- Files/folders: `scripts/core/Owner.cs`, `scripts/core/SkirmishOptions.cs`, `scripts/core/MatchConfig.cs`, `scripts/core/LaunchMode.cs`, `scripts/MainMenuRoot.cs`, `scripts/BattleRoot.cs`, `tools/CombatBehavior/Program.cs`, `tools/ReviewGate/Program.cs`, `TODO.md`, `docs/reviews/2026-06-29-mode-1v1-scope.md`.
- Non-goals: no gameplay balance change, no unit architecture change, no multiplayer implementation, no campaign implementation, no removal of future-reserved `PlayerSlotId` values.

Implementation summary:
- Added CombatBehavior checks proving `Owner` remains one human-facing player role plus one enemy AI role.
- Added CombatBehavior checks proving launch modes remain skirmish plus developer sandbox.
- Added CombatBehavior checks rejecting player-count or multiplayer config fields in `SkirmishOptions` and `MatchConfig`.
- Added CombatBehavior checks proving default skirmish maps the human player faction against the AI faction and that relation logic supports local slot One versus AI slot Two.
- Added `ReviewGate mode1v1` to guard menu, config, battle wiring, and absence of Godot multiplayer networking APIs.

Automated gates:
- Command: `dotnet build ProceduralRts.csproj --no-restore`
  Result: pass
  Evidence: build completed with 0 warnings and 0 errors.
- Command: `dotnet run --project tools/CombatBehavior/CombatBehavior.csproj --no-restore`
  Result: pass
  Evidence: CombatBehavior passed, including 1v1 mode, launch mode, config, and relation checks.
- Command: `dotnet run --project tools/ReviewGate/ReviewGate.csproj mode1v1 --no-restore`
  Result: pass
  Evidence: ReviewGate reported 0 errors and 0 warnings for the 1v1 scope lock.

Manual/visual gates:
- Check: visual QA
  Result: not run
  Evidence: this is a scope/configuration guard; no rendering or UI layout changed.

Reviewer result:
- Status: pass
- Required fixes: first static scan used a broad `ENet` substring and falsely matched UI code; narrowed to concrete Godot multiplayer API tokens.
- Residual risks: future `PlayerSlotId.Three/Four` placeholders still exist by design, but no current menu, config, battle flow, or networking API exposes multiplayer. Campaign remains a separate open scope-lock item.

TODO update:
- Items marked done: `Mode: 1v1 only - one human player vs one computer AI. No multiplayer netcode yet`.
- Items left open: Dog/Cat fully playable, campaign out of scope, player loop, AI command-buffer visibility, counters, performance/readability, and entity authority migration.
- Reason: automated gates now prove the current executable slice is local human player versus computer AI, with no multiplayer netcode entry points.
